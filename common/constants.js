const ActionResult = {
    SUCCESS: 'success',
    FAILURE: 'failure',
};

const ActionType = {
    INVITATION: 'invitation',
    SENDING: 'sending',
};

const ScheduleStatus = {
    PENDING: 'pending',
    SENT: 'sent',
};

module.exports = {
    ActionResult,
    ActionType,
    ScheduleStatus,
};
